#include "sort.h"

/**
 * bitonic_sort - sorts an array of integers with shell sorting
 *
 * @array: array
 * @size: size of the array
 * Return: Void
 */
void bitonic_sort(int *array, size_t size)
{
	if (size < 2 || !array)
		return;
}


