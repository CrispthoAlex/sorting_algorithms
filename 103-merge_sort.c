#include "sort.h"

/**
 * merge_sort - sorts an array of integers with merge sorting
 *
 * @array: array
 * @size: size of the array
 * Return: Void
 */
void merge_sort(int *array, size_t size)
{
	if (size < 2 || !array)
		return;
}


